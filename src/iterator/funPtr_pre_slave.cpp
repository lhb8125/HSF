/**
* @file: funPtr_pre_slave.cpp
* @author: Liu Hongbin
* @brief: 
* @date:   2019-10-09 11:04:42
* @last Modified by:   lhb8125
* @last Modified time: 2020-02-23 15:42:43
*/

#include "funPtr_slave.hpp"

void loop_pre()
{
    // Field<scalar> &fieldA = reg.getField<scalar>(A);
    // Field<scalar> &fieldx = reg.getField<scalar>(x);
    // Field<scalar> &fieldb = reg.getField<scalar>(b);

    // Array<label32> inoutList;
    // inoutList.push_back(1); // A: copyin
    // inoutList.push_back(1); // x: copyin
    // inoutList.push_back(3); // b: copyinout

    // prepareField(&inoutList[0], "spMV", 3, &fieldA, &fieldx, &fieldb);
}
